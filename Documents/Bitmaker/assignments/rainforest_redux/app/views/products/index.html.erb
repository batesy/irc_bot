
<h1>Products</h1>
<%= link_to "Add Product!", new_product_path  %>

<% @products.each do |product| %>
  <div class="product">
    <h2><%= link_to product.name, "/products/#{product.id}" %></p>
  </div>
<% end %>
