<% @products.each do |p| %>
<div class="product">
<h2><%= link_to p.name, "/products/#{p.id}" %></h2>
<%= link_to image_tag p.photo(:thumb) %>
<p><%= p.formatted_price %></p>
</div>
<% end %>

<h3><%= link_to "Create New Product", new_product_url %>